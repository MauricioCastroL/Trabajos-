inversor = float(input("Caunto inviertio: "))
inversor1 = float(input("Caunto inviertio: "))
inversor2 = float(input("Caunto inviertio: "))
resolver = (inversor/(inversor+inversor1+inversor2))*100
resolver1 = (inversor1/(inversor+inversor1+inversor2))*100
resolver2 = (inversor2/(inversor+inversor1+inversor2))*100
print("La cantidad que recibe el primer, segundo y tercer inversor respectivamente es: ", resolver, resolver1, resolver2)
